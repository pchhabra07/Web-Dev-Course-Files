//Once a variable is declared with a type (let,,var or const), it cannot be 
// re-declared but if no type is given, then a type is given further, that is okay.

x=10;
console.log(x);

var x=12;
console.log(x);